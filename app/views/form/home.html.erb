<% provide(:xx ,"主页") %>
<div class="center hero-unit">
<h1>动态表单
</h1>

<% if signed_in? %>
     <p>欢迎您</p>
     <%= current_user.name%>
     <p>
     <%= link_to '退出',logout_path%></p>
     <div>
     <tr><%= link_to '创建表单', new_path %></tr>
<tr><%= link_to '我的表单', show_path %></tr>
     </div>
<% else %>
    <%= link_to '登陆' , login_path%>
<% end %>
<ul>

</ul>
</div>